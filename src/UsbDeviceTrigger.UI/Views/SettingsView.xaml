<UserControl x:Class="UsbDeviceTrigger.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- Header -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Paramètres"
                           Style="{StaticResource SectionHeader}"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="🔄 Réinitialiser"
                            Command="{Binding ResetSettingsCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Foreground="#D32F2F"/>

                    <Button Content="💾 Sauvegarder"
                            Command="{Binding SaveSettingsCommand}"
                            Style="{StaticResource PrimaryButton}"/>
                </StackPanel>
            </Grid>

            <!-- Démarrage et comportement -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="⚙ Démarrage et comportement"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Margin="0,0,0,15"/>

                    <CheckBox Content="Démarrer avec Windows"
                              IsChecked="{Binding StartWithWindows}"
                              Margin="0,5"/>

                    <CheckBox Content="Démarrer minimisé dans la barre d'état"
                              IsChecked="{Binding StartMinimized}"
                              IsEnabled="{Binding StartWithWindows}"
                              Margin="20,5,0,5"/>

                    <CheckBox Content="Minimiser vers la barre d'état système au lieu de fermer"
                              IsChecked="{Binding MinimizeToTray}"
                              Margin="0,5"/>

                    <CheckBox Content="Démarrer automatiquement la surveillance USB au lancement"
                              IsChecked="{Binding AutoStartMonitoring}"
                              Margin="0,5"/>
                </StackPanel>
            </Border>

            <!-- Notifications -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="🔔 Notifications"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Margin="0,0,0,15"/>

                    <CheckBox Content="Afficher les notifications lors des événements USB"
                              IsChecked="{Binding ShowNotifications}"
                              Margin="0,5"/>

                    <TextBlock Text="Les notifications vous informent lorsqu'un périphérique configuré se connecte ou se déconnecte, ainsi que du résultat de l'exécution des commandes."
                               TextWrapping="Wrap"
                               Foreground="#666666"
                               FontSize="12"
                               Margin="20,5,0,10"/>
                </StackPanel>
            </Border>

            <!-- Journalisation -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="📝 Journalisation"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Margin="0,0,0,15"/>

                    <CheckBox Content="Enregistrer l'exécution des commandes dans un fichier log"
                              IsChecked="{Binding LogCommandExecution}"
                              Margin="0,5,0,15"/>

                    <Grid IsEnabled="{Binding LogCommandExecution}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0"
                                 Text="{Binding LogFilePath, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,5"
                                 Padding="10"
                                 Height="40"/>

                        <Button Grid.Column="1"
                                Content="📁 Parcourir"
                                Command="{Binding BrowseLogPathCommand}"
                                Style="{StaticResource SecondaryButton}"
                                Margin="10,5,0,5"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Configuration -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="📂 Configuration"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Fichier de configuration:"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,5"/>

                            <TextBlock Text="{Binding ConfigFilePath}"
                                       TextWrapping="Wrap"
                                       FontFamily="Consolas"
                                       FontSize="12"
                                       Foreground="#666666"/>
                        </StackPanel>

                        <Button Grid.Column="1"
                                Content="📁 Ouvrir le dossier"
                                Command="{Binding OpenConfigFolderCommand}"
                                Style="{StaticResource SecondaryButton}"
                                VerticalAlignment="Center"/>
                    </Grid>

                    <TextBlock Text="Le fichier de configuration contient tous vos périphériques configurés et leurs commandes. Vous pouvez le sauvegarder pour le restaurer ultérieurement."
                               TextWrapping="Wrap"
                               Foreground="#666666"
                               FontSize="12"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- À propos -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="ℹ À propos"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Margin="0,0,0,15"/>

                    <TextBlock Text="USB Device Trigger"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="0,0,0,5"/>

                    <TextBlock Text="Version 1.0.0"
                               FontSize="14"
                               Foreground="#666666"
                               Margin="0,0,0,10"/>

                    <TextBlock TextWrapping="Wrap"
                               Margin="0,0,0,10">
                        <Run Text="Application permettant d'exécuter des commandes Windows automatiquement lors de la connexion ou déconnexion de périphériques USB."/>
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap"
                               FontSize="12"
                               Foreground="#666666">
                        <Run Text="Développé avec:"/>
                        <LineBreak/>
                        <Run Text="• C# WPF (.NET 8.0)"/>
                        <LineBreak/>
                        <Run Text="• Material Design Themes"/>
                        <LineBreak/>
                        <Run Text="• Community Toolkit MVVM"/>
                    </TextBlock>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
